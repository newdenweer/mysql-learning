FROM flyway/flyway:7.5

COPY ./migrations /flyway/sql

ARG DB_HOST
ARG DB_NAME
ARG DB_USER
ARG DB_PWD

ENV FLYWAY_URL jdbc:mysql://$DB_HOST/$DB_NAME
ENV FLYWAY_USER $DB_USER
ENV FLYWAY_PASSWORD $DB_PWD
ENV FLYWAY_CONNECT_RETRIES 60
ENV FLYWAY_BASELINE_ON_MIGRATE true
